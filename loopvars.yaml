- name: Install packages using variables
  hosts: all
  become: yes

  vars:
    packages:
      - nginx
      - git
      - curl

  tasks:
    - name: Install packages
      apt:
        name: "{{ item }}"
        state: present
      loop: "{{ packages }}"